query Input {
  cart {
    lines {
      id
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          id
          title
          product {
            title
            hasAnyTag(tags: ["tier-discount"])
          }
        }
      }
    }
  }
}
